apiVersion: v1
kind: Service
metadata:
  name: cireleasechecklist-service
  namespace: devops
spec:
  selector:
    app: cireleasechecklist
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501
    - protocol: TCP
      port: 8510
      targetPort: 8510
  type: ClusterIP
